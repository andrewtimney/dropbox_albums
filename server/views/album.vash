@html.extend('layout', function(model){

    @html.block('content', function(model){

		<h2><a href="/o?i=@model.album.id">@model.album.title</a></h2>

    <hr />

		@model.album.files.forEach(function(file){
			<img src="@file" width="150" height="150" />
		})
	})

})
